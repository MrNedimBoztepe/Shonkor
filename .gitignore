# .NET
bin/
obj/
*.user
*.suo
*.cache
*.dll
*.exe
*.pdb
*.nupkg
*.snupkg

# IDE
.vs/
.vscode/
.idea/
*.swp

# Shonkor Runtime
.shonkor/brain.db
.shonkor/logs/
# Project registry contains API keys and machine-specific paths -> never commit.
projects.json
shonkor.db
*.db
*.db-journal
*.db-wal
*.db-shm
test-capsule.md
shonkor-capsule.md

# Build output & throwaway/scratch projects (not part of the source tree)
dist/
scratch/
publish/

# Test artifacts & local benchmark outputs (machine-specific paths, internal data)
test.json
*.benchmark.json

# Logs
*.log

# Runtime workspace plugins dir (standard plugins are regenerated from embedded resources;
# shippable plugins belong in src/Shonkor.Core/StandardPlugins instead).
plugins/

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Local NuGet pack output (dotnet tool)
nupkg/
*.nupkg
publish-test/
publish/

# Eval harness: keep golden set + baselines, ignore generated caches/reports
eval/cache-*.json
eval/experiment-*.json
eval/last-report.md
eval/last-metrics.json
